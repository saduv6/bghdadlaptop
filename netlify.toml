[build]
  publish = "dist"
  command = "npm run build"

# SPA-style routing so /checkout and /dashboard resolve to their HTML files
[[redirects]]
  from = "/checkout"
  to = "/checkout.html"
  status = 200

[[redirects]]
  from = "/dashboard"
  to = "/dashboard.html"
  status = 200
